# ============================================
# Environment Variables Sample File
# ============================================
# Copy this file to .env in the project root and update with your actual values
# DO NOT commit .env to version control
# 
# @author Thang Truong
# @date 2025-12-12
# ============================================

# ============================================
# BACKEND ENVIRONMENT VARIABLES
# ============================================

# Server Configuration
# Port for the Express backend server
PORT=5000

# Node Environment (development, production, test)
NODE_ENV=development

# Frontend URL for CORS and email links
# Used for CORS configuration and generating links in emails
FRONTEND_URL=http://localhost:3000

# ============================================
# Database Configuration (MySQL)
# ============================================

# MySQL database host
DB_HOST=localhost

# MySQL database user
DB_USER=root

# MySQL database password (leave empty if no password)
# Note: Do not add quotes around the password value
DB_PASSWORD=

# MySQL database name
DB_NAME=ecommerce_db

# ============================================
# JWT Authentication Configuration
# ============================================

# Secret key for signing JWT tokens
# IMPORTANT: Change this to a strong random string in production
# Generate a secure random string: openssl rand -base64 32
JWT_SECRET=your-secret-key-here-change-in-production

# Access token expiration time (e.g., 15m, 1h, 1d)
# Format: number followed by unit (s, m, h, d)
ACCESS_TOKEN_EXPIRY=15m

# Refresh token expiration time (e.g., 7d, 30d)
# Format: number followed by unit (s, m, h, d)
REFRESH_TOKEN_EXPIRY=7d

# ============================================
# Email Configuration (SMTP)
# Used for sending password reset emails and invoice emails
# ============================================

# SMTP server host (e.g., smtp.gmail.com, smtp.sendgrid.net)
SMTP_HOST=smtp.gmail.com

# SMTP server port (587 for TLS, 465 for SSL)
SMTP_PORT=587

# Use secure connection (true for SSL on port 465, false for TLS on port 587)
SMTP_SECURE=false

# SMTP authentication username (usually your email address)
SMTP_USER=your-email@gmail.com

# SMTP authentication password (for Gmail, use App Password, not regular password)
# For Gmail: Generate App Password at https://myaccount.google.com/apppasswords
SMTP_PASS=your-app-password

# Email sender address (displayed in "From" field)
EMAIL_FROM=noreply@ecommerce.com

# Email sender name (displayed in "From" field)
EMAIL_FROM_NAME=Ecommerce Store

# Logo URL for email templates
# Can be absolute URL (https://yourdomain.com/Logo.png) or relative to frontend
# If relative, will be combined with FRONTEND_URL
EMAIL_LOGO_URL=http://localhost:3000/Logo.png

# ============================================
# File Upload Configuration
# ============================================

# Maximum file upload size in bytes (default: 5MB = 5242880)
# 5MB = 5242880, 10MB = 10485760, 20MB = 20971520
UPLOAD_MAX_SIZE=5242880

# Allowed image MIME types (comma-separated)
UPLOAD_ALLOWED_IMAGE_TYPES=image/jpeg,image/png,image/gif,image/webp

# Allowed video MIME types (comma-separated)
UPLOAD_ALLOWED_VIDEO_TYPES=video/mp4,video/webm,video/ogg

# ============================================
# FRONTEND ENVIRONMENT VARIABLES
# ============================================
# Note: Vite uses VITE_ prefix for environment variables
# Access in frontend code: import.meta.env.VITE_API_URL
# ============================================

# API Base URL (optional - defaults to /api proxy in vite.config.js)
# Only needed if frontend and backend are on different domains
# VITE_API_URL=http://localhost:5000/api

# ============================================
# NOTES
# ============================================
# 1. For Gmail SMTP:
#    - Enable 2-factor authentication
#    - Generate App Password: https://myaccount.google.com/apppasswords
#    - Use the App Password as SMTP_PASS
#
# 2. For production:
#    - Set NODE_ENV=production
#    - Use strong JWT_SECRET (generate with: openssl rand -base64 32)
#    - Update FRONTEND_URL to your production domain
#    - Update EMAIL_LOGO_URL to your production logo URL
#    - Use secure SMTP settings (SMTP_SECURE=true, SMTP_PORT=465)
#
# 3. Database:
#    - Ensure MySQL is running
#    - Create database: CREATE DATABASE ecommerce_db;
#    - Run schema: See database/schema.sql
#
# 4. Security:
#    - Never commit .env file to version control
#    - Keep JWT_SECRET secure and unique
#    - Use environment-specific values for production
# ============================================
